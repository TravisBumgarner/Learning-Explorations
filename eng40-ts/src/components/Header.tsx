import * as React from 'react'

const wrapperStyle = { color: 'red' }

const Header = () => {
    return (
        <div style={wrapperStyle}>
            <h1>Todo List App</h1>
        </div>
    )
}

export default Header
