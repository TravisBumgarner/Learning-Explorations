{
  "name": "commit_or_revert",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "gamble": "git add . && (npm run test && git commit -m $1) || (git reset --hard && git clean -f -d)"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^5.2.0"
  }
}
