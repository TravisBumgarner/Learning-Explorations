{
  "name": "mock-db",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names \"db,temporal,server\" \"docker compose up\" \"npm run dev -w @mock-db/temporal\" \"npm run dev -w @mock-db/server\"",
    "build": "npm run build --workspaces",
    "test": "npm run test -w @mock-db/temporal"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
