
// Reduce

x.reduce((accumulator, currentValue) => {
    // do things
    return accumulator
}, {})

// Sort

/*
    compareFunction(a, b) returns
        < 0 -> a comes before b
        = 0 -> no change
        > 0 -> b comes before a
*/